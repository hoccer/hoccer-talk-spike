<?xml version="1.0" encoding="utf-8"?>
<!-- package name must be unique so suffix with "tests" so package loader doesn't ignore us -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.hoccer.xo.release.tests"
          android:versionCode="1"
          android:versionName="1.0">

  <!-- We run on 4.0 (14) and up, we use features from up to 4.2 (17) -->
  <uses-sdk
      android:minSdkVersion="14"
      android:targetSdkVersion="17"/>

  <!-- We add an application tag here just so that we can indicate that
       this package needs to link against the android.test library,
       which is needed when building test cases. -->
  <application>
    <uses-library android:name="android.test.runner"/>
  </application>
  <!--
  This declares that this application uses the instrumentation test runner targeting
  the package of com.hoccer.xo.release.tests.  To run the tests use the command:
  "adb shell am instrument -w com.hoccer.xo.release.tests.tests/android.test.InstrumentationTestRunner"
  -->

  <!-- Espresso requires GoogleInstrumentationTestRunner -->
  <!--<instrumentation-->
      <!--android:name="com.google.android.apps.common.testing.testrunner.GoogleInstrumentationTestRunner"-->
      <!--android:targetPackage="com.hoccer.xo.release"/>-->

  <instrumentation
      android:name="android.test.InstrumentationTestRunner"
      android:targetPackage="com.hoccer.xo.release"/>

</manifest>
