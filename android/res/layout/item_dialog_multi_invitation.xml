<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">
    <QuickContactBadge
            android:id="@+id/cb_quickcontact"
            android:layout_height="60dp"
            android:layout_width="60dp"
            android:scaleType="centerCrop"/>

    <TextView android:id="@+id/tv_displayname"
              android:layout_width="match_parent"
              android:layout_height="30dp"
              android:layout_toRightOf="@+id/cb_quickcontact"
              android:layout_toLeftOf="@+id/checkBox"
              android:gravity="center_vertical"
              android:textSize="18sp"
              android:paddingLeft="@dimen/profile_text_margin"
              android:paddingRight="@dimen/profile_text_margin"
              android:layout_alignParentTop="true"/>

    <TextView android:id="@+id/tv_detailed_info"
              android:layout_width="match_parent"
              android:layout_height="30dp"
              android:layout_toRightOf="@+id/cb_quickcontact"
              android:layout_toLeftOf="@+id/checkBox"
              android:layout_below="@+id/tv_displayname"
              android:gravity="center_vertical"
              android:textSize="18sp"
              android:paddingLeft="@dimen/profile_text_margin"
              android:paddingRight="@dimen/profile_text_margin"
              android:singleLine="true"
              android:ellipsize="marquee"
              android:marqueeRepeatLimit ="marquee_forever"
              android:scrollHorizontally="true"
              android:focusable="true"
              android:focusableInTouchMode="true"/>

    <CheckBox
            android:id="@+id/checkBox"
            android:layout_width="wrap_content"
            android:layout_height="60dp"
            android:layout_alignParentTop="true"
            android:layout_alignParentRight="true"
            android:paddingRight="5dp"/>
</RelativeLayout>